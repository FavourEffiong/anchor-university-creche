# Anchor University Creche

This project now includes a PHP + MySQL backend with authentication and role-based access for Admin, Staff, and Parent users. Features include registration, login, password reset (via Gmail SMTP), parent school fee payments, and admin management tools. All backend pages match the site theme.